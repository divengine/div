# 2.29 Pre-defined sub-parsers

Div includes a small set of built-in sub-parsers. The default one is:

- `{parse} ... {/parse}`: runs a pre-process step by creating a new div instance, similar to loops and capsules.
