# 2.16 Default replacement for a variable

Default replacements can be scoped to a specific variable.

## 6.16.1 PHP usage

```php
<?php

div::setDefaultByVar($varname, $value_to_search, $replace_with, $update);
```

## 6.16.2 Template usage

```
{@ ['varname', value_to_search, replace_with] @}
```