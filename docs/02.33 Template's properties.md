# 2.33 Template's properties

Templates can include metadata properties that apply only to the file in which they are defined.

**Syntax**

```html
@_property_name = property's value
```

Example

```html
@_DIALECT = smarty.dialect
```

Properties are identified by the `@_` prefix.
